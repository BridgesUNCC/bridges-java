import bridges.Retrieve;
import bridges.JSONElement;
import bridges.ReferenceStack; // This would be replaced by whatever the student made

// The student should be able to create this file

public class ReferenceStackDriver {
    public static void main(List<String> arguments) {
        ReferenceStack<JSONElement> structure = new ReferenceStack<JSONElement>();
        val ret = new Retrieve("geolist", is);
        ret.get_multiple();
    }
}